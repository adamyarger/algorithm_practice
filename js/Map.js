/**
 * store DOM elements as keys
 * es6 Map type allows this so the gaol is to build Map
 * 
 * can use symbol as key... but that doesnt work in es5
 * 
 * When to use Symbols? How do they work?
 */

function Map() {
  const KEY = '__index'
  const nodeList = []
  const valueList = []

  this.set = function (key, val) {

  }

  this.get = function (key) {

  }

  this.has = function (key) {

  }
}